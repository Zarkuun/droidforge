BUGS:
- Labels, die zu lang sind, sollen nicht in die zweite Zeile umbrechen,
  sondern abgeschnitten werden.
- Doppelklick auf Jackname soll den Kommentardialog aufmachen, nicht die Funktioun
  zum Taushen des Jacks.
- Wenn man ein Info auf einem Jack hat und eine Warnung auf Column 1 (Input) wird
  das problem eins zu weit links ausgegeben
- Text in der Statusbar: wenn dieser vom I oder vom Problem kommt, sollte das
  Icon davor stehen.
- Die Miniicons in der Dropdownliste im Cableselector sind noch die knalligen
- "Edit input parameter": Den Zusatztext über den Jacktyp hier auch nochmal anzeigen.
  Eventuell auch das Logo.
- Edit input parameter: Der Kabelselector soll die Breite bekommen, die dazukommt.
- Rackview bottom margin ist zu groß. Einsrtellung wirkt nicht.
- Ich kopiere was und kann nicht in eine Outputzeile pasten, weil der Cursor
  unsichtbar auf Spalte 2 steht
- Analog: Enter auf einem Output bringt den aktuellen Wert nicht in den
  Selektor wenn cursor auf Spalte 2 steht
- Jack chooser: Wenn wegen eines Filters keine Outputs da sind, und ich
  bin in einem Arrayinput mit 8 Inputs, kann ich in der rechtesten
  Spalte weiter nach rechts fahren und der Cursor geht dann irgendwo
  ins verborgene.
- Kabel umbenennen: Ich kann nicht in der Mitte vom Kabel editieren,
  denn der Cursor spring immer an das Ende, nach jedem Zeichen
- Wenn ich einen Circuit mit CMD-U im Sourcecode editiere, geht 
  die erste Zeile vom Kommentar weg (wird wohl als Title gewertet)


TODO:

- Doch vielleicht einbauen, das man ein Rack speichern kann (Rackview:
  "Save rack..."). Und das in einem Menü dann direkt holen kann.
  Oder was weiß ich.
- Wenn man durch Mehrfachklicken aus einem B ein L macht, dann soll ein
  Hint kommen.
- Wenn ich auf einer Zell mit einem Register stehe und ? drücke, soll
  ich zu allen anderen Registern weiterhüpfen, die verwandt sind.
  Damit kann man dann auch die angefangene Suche fortsetzen

- Eventuell die Hilitefarbe des Registers an die Farbe vom aktuellen Circuit
  anpassen.

- Hilfemenü: Link zu Discord einbauen.

- Wir müssen noch entscheiden, wie wir die Forge-Releases mit den Firmware-Releases
  verheiraten. Hat eine Forge immer genau eine Firmware? Dann müssten wir die Forge
  eigentlich immer gleichzeitig rausbringen.
  Release erzeugen. Dann die JSON-Datei übertragen und die Forge auch erzeugen.

- Mac: Das DMG hat kein Icon. Mit "Info" kann man ein Icon reindraggen
  (links oben). Ich weiß aber nicht, ob man danach nochmal notarizen muss.

- Die Doppelklickzeit vom Mac wird von den styleHints() nicht
  korrekt zurückgegeben. Ist immer 400. Das ist auch sehr lang.
  Deswegen verwende ich einfach so 200. Wir müssen eine Methode
  finden, den echten Wert rauszubeikommen. Und das einbauen
  in MouseDragger::doubleClickTime()

WINDOWS:

- Windows: Das Icon wird noch nicht genommen.

- Windows: Etliche Shortcuts gehen nicht, z.B. Save as... Aber auch sachen mit der
  Windowstaste, wo dann irgende Windowspezialfunktion aufpoppt.
  Konnte sein, dass ich hier doch teils unterschiedliche Shortcuts fuer
  die Betriebssysteme brauche.

